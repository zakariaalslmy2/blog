    // دمج معرف المستخدم الحالي مع بيانات الطلب
    $data = array_merge($request->except('image', '_token'), ['user_id' => auth()->id()]);




    المشكلة هي أن ملف blade يحتوي على سطر عرض العنوان (<a class="h6 m-0" ...>)، لكن النتيجة النهائية في المتصفح (كما في الصورة) لا تعرضه.
هذا يعني أن المشكلة ليست في الكود نفسه، بل في أحد الأسباب التالية (وهي شائعة جداً في بيئة Laravel):
